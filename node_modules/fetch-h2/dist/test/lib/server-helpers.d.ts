import { ServerOptions, TestData } from "./server-common";
export declare function makeMakeServer({ proto, version }: TestData): {
    makeServer: (opts?: ServerOptions | undefined) => Promise<{
        server: import("./server-common").Server;
        port: number | null;
    }>;
    cycleOpts: {
        httpProtocol: import("../..").HttpProtocols;
        httpsProtocols: import("../..").HttpProtocols[];
        session: {
            rejectUnauthorized: boolean;
        };
    };
};
