/// <reference types="node" />
import { Server as HttpServer } from "http";
import { Server as HttpsServer } from "https";
import { Server, ServerOptions, TypedServer } from "./server-common";
export declare class ServerHttp1 extends TypedServer<HttpServer | HttpsServer> {
    private _store;
    constructor(opts: ServerOptions);
    _shutdown(): Promise<void>;
    private onRequest;
}
export declare function makeServer(opts?: ServerOptions): Promise<{
    server: Server;
    port: number | null;
}>;
